import { getAnalytics } from "firebase/analytics";
import { useFirebaseApp } from "../app/composables.js";
export function useAnalytics() {
  return import.meta.client ? getAnalytics(useFirebaseApp()) : null;
}
